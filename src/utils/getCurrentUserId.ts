/**
 * Returns the current user's ID.
 * Currently returns a static demo user.
 * Replace this with actual auth logic when authentication is implemented.
 */
export function getCurrentUserId(): string {
  return "demo-user";
}
