# Theme toggle header fix

The theme toggle should appear in the existing top-right app header beside notifications and profile controls. It should use the existing theme provider and preserve the premium Home layout. This is an implementation note while the exact header component path is resolved.