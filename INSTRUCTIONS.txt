STUDYSPHERE — VOCABULARY BANK FIX
==================================

PROBLEM FOUND:
Vocabulary auto-generation code already existed in your repo
(src/app/api/mission-ias/generate-vocabulary/route.ts) but it was NEVER
being triggered — it wasn't in vercel.json's cron list, and there was no
button anywhere to run it manually. So the vocabulary bank stayed empty.

WHAT CHANGED (3 files):
1. src/app/api/mission-ias/generate-vocabulary/route.ts
   - Refactored so the core "ask Gemini for 20 new words, save unique ones"
     logic is shared.
   - GET  = used by Vercel Cron (automatic, once a day, protected by
     CRON_SECRET, same pattern as your existing fetch-news route).
   - POST = NEW. Used by a button in the UI. Protected by your normal
     Firebase login (any signed-in user can trigger it — you're the only
     user of this app).

2. src/app/(dashboard)/dashboard/mission-ias/vocabulary-lab/page.tsx
   - Added a "Generate More Words" button next to Browse/Quiz toggle.
     Click it anytime to add ~20 new UPSC-level words immediately
     (skips duplicates automatically).

3. vercel.json
   - Added a second cron entry so generate-vocabulary now runs
     automatically every day at 4 AM UTC (fetch-news already runs at 3 AM),
     same secure pattern (Vercel sends CRON_SECRET as a Bearer header
     automatically — nothing exposed in the URL).

WHAT TO DO:
1. Extract this ZIP, copy the "src" folder and "vercel.json" into your
   project root (overwrite when asked — only 3 files touched, nothing
   else affected).
2. Make sure these two env vars are set in Vercel project settings
   (Production + Preview):
     - GEMINI_API_KEY   (already needed for the rest of English Lab —
       should already be set)
     - CRON_SECRET      (any random string you choose, e.g. a UUID —
       check if you already have one set for fetch-news; if yes, reuse it)
3. git add .
4. git commit -m "Fix vocabulary auto-generation: add cron + manual trigger button"
5. git push
6. After deploy, open Vocabulary Lab in the app and click
   "Generate More Words" a few times to seed the bank right away
   (each click adds up to 20 new unique words, ~5-10 seconds each).
   After that, it'll also keep growing on its own every day automatically.

Compile-checked with `npx tsc --noEmit` — no errors in the changed files.

==================================
BONUS FIX — Map Practice page.tsx
==================================
Your screenshot showed 4 TypeScript errors in map-practice/page.tsx:
"Cannot find name 'normalizeName'" and "Cannot find name 'getFeatureName'".

ROOT CAUSE: the GeoJSON file (public/data/india-states.geojson) is actually
DISTRICT-level (759 features), not state-level. Its properties look like
{ st_nm: "Mizoram", district: "Aizawl", dt_code: "261", ... } — there's no
"name" field. The page's type declared `{ name: string }` and called two
helper functions that were never written, so it never compiled.

FIXED:
- Added getFeatureName() — reads st_nm (falls back to name/NAME_1 for
  future-proofing).
- Added normalizeName() — trim + lowercase for safe comparisons (the st_nm
  values already match your INDIA_STATES list exactly, so no fuzzy matching
  needed).
- Fixed the StateFeature type to reflect the real properties shape.
- De-duplicated state names before starting the "Identify Quiz" — since the
  file has ~20 district polygons per state, without this a state could be
  asked multiple times in one round.
- Fixed a React key collision — every district polygon of the same state
  was using the state name as its key; now uses dt_code (unique per
  district) instead.

This file is now added to the ZIP (4th file total). Same steps apply — just
copy the src folder over and it's included automatically.
