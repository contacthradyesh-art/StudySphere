STUDYSPHERE — MAP PRACTICE: RIVERS & MOUNTAINS OVERLAY
========================================================

CONTEXT: This finishes what an earlier (message-limited) session started —
it had written the plan/code but ran out of free messages before pushing.
I re-verified everything against your ACTUAL current repo (not the pasted
plan) and merged it in properly, since your live map-practice/page.tsx
already had things the plan didn't know about (Timed Challenge mode,
color palette, search) — a blind copy-paste would have deleted those.
I only added new code around them.

WHAT'S NEW:
1. src/lib/mission-ias/geography-schema.ts (NEW FILE)
   - RIVERS: 10 major Indian rivers (Ganga, Yamuna, Brahmaputra, Indus,
     Godavari, Krishna, Kaveri, Narmada, Tapi, Mahanadi) with origin,
     length, outflow, states, tributaries, UPSC-relevant significance.
   - MOUNTAINS: 7 ranges (Himalayas, Karakoram, Aravalli, Vindhya, Satpura,
     Western Ghats, Eastern Ghats) with verified peak coordinates.

2. public/data/world-rivers.geojson (NEW FILE, ~125 KB)
   - Real river-line geometry from Natural Earth (public domain), filtered
     down to only the rivers above (not the full 7MB dataset).
   - HONEST GAP: Natural Earth's global rivers layer does NOT include the
     Godavari or Mahanadi at all (checked directly — genuinely absent from
     the source data, not a matching bug). So 8 of the 10 rivers render as
     real lines on the map; Godavari and Mahanadi have full info in the
     schema (for AI-assisted facts) but no line yet. If you want those two
     added later, they'd need a different/higher-detail river dataset.

3. src/app/(dashboard)/dashboard/mission-ias/map-practice/page.tsx (UPDATED)
   - Added a "Rivers & Mountains" toggle (only visible in India + Explore
     mode). Blue lines = rivers, amber dots = mountain peaks — click either
     for a detail panel + "Ask AI" button, same pattern as clicking a state.
   - Your existing Timed Challenge mode, color palette, search, and World
     tab are untouched.
   - Bonus fix: the right-side info panel had no message during Timed
     Challenge mode (only Identify Quiz showed one) — fixed.

4. scripts/fetch-map-data.mjs (UPDATED)
   - Added the rivers-fetching step, so you (or I) can regenerate
     world-rivers.geojson from scratch later if needed:
     node scripts/fetch-map-data.mjs

DELETE THIS (ZIP overwrite can't remove files, has to be manual):
  src/app/mission-ias/  (the WHOLE folder, not the (dashboard) one)
  This is a leftover orphan route from the very first Map Practice commit,
  outside the (dashboard) layout group. Nothing links to it, but it still
  gets type-checked on every build and carries old broken code — safe and
  good practice to delete it.

WHAT TO DO:
1. Extract ZIP, copy src/, public/, scripts/ into your project (overwrite
   when asked)
2. Manually delete src/app/mission-ias/ (see above)
3. git add .
4. git commit -m "Add Rivers & Mountains overlay to Map Practice"
5. git push
6. After deploy: Mission IAS -> Map Practice -> India tab -> Explore mode
   -> click "Rivers & Mountains" -> click a blue line or amber dot
